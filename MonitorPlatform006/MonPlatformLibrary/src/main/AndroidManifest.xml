<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.boha.monitor.library" >

    <application
        android:allowBackup="true"
        android:label="@string/app_name" >

        <service android:name=".services.GPSService"/>
        <service android:name=".services.PhotoUploadService"/>
        <service android:name=".services.RequestSyncService"/>
        <service android:name=".services.StatusSyncService"/>

        <receiver android:name=".services.LocationTrackerReceiver"/>
        <receiver android:name=".services.BootReceiver"/>

    </application>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

</manifest>
